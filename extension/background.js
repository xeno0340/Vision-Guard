// VisionGuard - background.js (service worker)
//
// Step 2: not doing anything yet - popup.js handles capture directly.
// This file exists now so the manifest is valid and so later steps
// (DOM detection coordination, server calls, etc.) have a home
// without needing to restructure the extension.

console.log("[VisionGuard] background service worker loaded");